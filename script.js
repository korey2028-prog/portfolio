(()=>{'use strict';const header=document.querySelector('[data-header]');const year=document.querySelector('[data-year]');const printButton=document.querySelector('[data-print]');if(year)year.textContent=new Date().getFullYear();if(printButton)printButton.addEventListener('click',()=>window.print());const updateHeader=()=>header?.classList.toggle('scrolled',window.scrollY>16);updateHeader();window.addEventListener('scroll',updateHeader,{passive:true});const items=document.querySelectorAll('.reveal');if(!('IntersectionObserver'in window)){items.forEach((item)=>item.classList.add('visible'));return}const observer=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{if(!entry.isIntersecting)return;entry.target.classList.add('visible');observer.unobserve(entry.target)})},{threshold:.1});items.forEach((item)=>observer.observe(item))})();
